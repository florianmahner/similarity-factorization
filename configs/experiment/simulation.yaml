# @package _global_
experiment_name: "simulation"
task: ???

seed: 42
n_jobs: -1

interpretability:
  n: 200
  k: 8
  snrs: [1.0, 0.8, 0.6, 0.4, 0.2]
  ccc_runs: 5
  alpha_grid: [0.1, 0.3, 0.5, 1.0, 2.0, 5.0, 10.0]
  kernel: linear
  output_dir: "${project_root}/experiments/simulation/outputs/interpretability"

rank_detection:
  n: 200
  true_ranks: [5, 10, 15, 20, 25, 30]
  snrs: [0.4, 0.6, 0.8, 1.0]
  n_seeds: 5
  cv_repeats: 5
  grid_span: 10
  n_jobs: -1
  output_dir: "${project_root}/experiments/simulation/outputs/rank_detection"

imputation:
  n: 200
  k: 5
  kernel: gaussian_kernel
  fraction_retained: [0.01, 0.05, 0.1, 0.2, 0.4, 0.6, 0.8, 0.9]
  n_datasets: 10
  n_replicates: 5
  output_dir: "${project_root}/experiments/simulation/outputs/imputation"
