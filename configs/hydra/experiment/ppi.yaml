# @package _global_
experiment_name: "ppi"
task: ???

# Shared settings (formerly common)
n_jobs: -1
seed: 0
rank: 30
# methods: ["cn", "aa", "ra", "jc", "srf"]
# methods: ["node2vec"]
# methods: ["seal"]
methods: ["srf"]

link_prediction:
  base_dir: "${paths.root_dir}"
  data_dir: "${paths.data_dir}/ppi"
  dataset: "STRING_human_min900_v12"
  splits_dir: "${paths.ppi.splits}"
  n_folds: 10
  neg_ratio: 1
  embedding_dim: 128
  embedding_epochs: 5
  # Data paths
  string_data_dir: "${paths.ppi.string}"
  c_elegans_data_dir: "${paths.ppi.c_elegans}"
  huri_data_dir: "${paths.ppi.huri}"

corum_validation:
  string_data: null
  string_data_dir: "${paths.ppi.string}"

node_classification:
  string_data: "${paths.ppi.string}"
  string_data_dir: "${paths.ppi.string}"
  cache_dir: "experiments/ppi/cache"
  n_runs: 5
  test_ratio: 0.2
