## RST-002: Create structural verification tests

### CRITICAL: NO FUNCTIONAL CHANGES
This ticket creates tests only, no code changes.

### Goal
Create tests that verify imports work, to catch breakage during restructuring.

### Files to Create
- `tests/test_structure.py`

### Implementation
```python
"""Structural verification tests for repository reorganization."""
import importlib
import subprocess
import sys
from pathlib import Path

import pytest


def test_src_imports():
    """Verify src/ modules are importable."""
    modules = [
        "tools.rsa",
        "tools.metrics",
        "utils.helpers",
        "utils.graphs",
    ]
    for mod in modules:
        importlib.import_module(mod)


def test_pysrf_import():
    """Verify pysrf is importable."""
    import pysrf
    from pysrf import SRF


def test_hydra_configs_valid():
    """Verify base Hydra config loads."""
    from omegaconf import OmegaConf

    project_root = Path(__file__).parent.parent
    cfg = OmegaConf.load(project_root / "configs" / "base.yaml")
    assert "seed" in cfg
```

### Testing
- [ ] `poetry run pytest tests/test_structure.py -v`

### Acceptance Criteria
- [ ] All structural tests pass
- [ ] Tests can be run as baseline before other tickets
