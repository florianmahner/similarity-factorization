## RST-013: Update CLAUDE.md and documentation

### CRITICAL: NO FUNCTIONAL CHANGES
This ticket updates documentation only.

### Goal
Update all documentation to reflect new structure.

### Files to Modify
- `CLAUDE.md`
- `README.md` (if exists and has structure info)

### Key Updates

1. **Directory names**:
   - `development/` -> `sandbox/`
   - `experiments/` -> `projects/`

2. **Structure description**:
   - Remove references to `lib/` and `tasks/` subfolders
   - Show flattened project structure

3. **Commands**:
   - Update example commands to use new paths
   - `./scripts/submit projects/ppi/link_prediction.py`
   - `./scripts/submit sandbox/my_experiment/run.py`

4. **Output locations**:
   - Projects: `outputs/projects/<name>/<task>/`
   - Sandbox: `sandbox/<name>/outputs/` (local)

5. **Workflow description**:
   - Sandbox is for AI-generated experimental code
   - Projects is for stable scientific work

### Testing
- [ ] All example commands in docs work
- [ ] No references to old structure (experiments/, development/, lib/, tasks/)

### Acceptance Criteria
- [ ] CLAUDE.md fully updated
- [ ] README.md updated (if applicable)
- [ ] All paths in documentation are correct
