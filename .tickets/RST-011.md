## RST-011: Simplify scripts/submit for new structure

### CRITICAL: NO FUNCTIONAL CHANGES
This ticket updates the submit script paths only.

### Goal
Update submit script to handle projects/ and sandbox/ paths.

### Files to Modify
- `scripts/submit`

### Key Changes
1. Replace "experiments" detection with "projects"
2. Replace "development" with "sandbox"
3. Simplify path detection logic
4. Update cleanup paths

### Implementation Details
In `scripts/submit`, update:

```python
# Line ~109: Change "experiments" to "projects"
if "projects" in parts:  # was: "experiments"

# Line ~123-124: Change experiment detection
if len(parts) >= 2 and parts[0] in {"project", "projects"}:  # was: experiment, experiments

# Line ~137: Update session name prefix if desired
session_name = f"srf_{script_path.parent.name}"  # was: gemini_
```

### Testing
- [ ] `./scripts/submit projects/ppi/link_prediction.py --help` works
- [ ] `./scripts/submit sandbox/test/run.py --help` works (if sandbox experiment exists)
- [ ] Background mode: `./scripts/submit projects/ppi/link_prediction.py --bg`
- [ ] `poetry run pytest tests/test_structure.py -v`

### Acceptance Criteria
- [ ] Submit script handles both projects/ and sandbox/
- [ ] Background mode still works
- [ ] SLURM launcher still works
- [ ] Error messages reference correct paths
