## RST-005: Rename experiments/ -> projects/

### CRITICAL: NO FUNCTIONAL CHANGES
This ticket ONLY renames a directory. No code modifications.

### Goal
Rename experiments folder to projects for clarity.

### Commands to Execute
```bash
git mv experiments/ projects/
```

### Files to Modify (references only)
- `scripts/submit` - Update "experiments" string references
- `scripts/run_task.py` - Update import path
- `pyproject.toml` - Update package include
- `CLAUDE.md` - Update references

### Testing
- [ ] `ls projects/` shows previous experiments/ contents
- [ ] `poetry run pytest tests/test_structure.py -v`

### Acceptance Criteria
- [ ] `projects/` exists with all previous contents
- [ ] `experiments/` no longer exists
