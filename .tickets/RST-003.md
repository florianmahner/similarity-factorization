## RST-003: Rename development/ -> sandbox/

### CRITICAL: NO FUNCTIONAL CHANGES
This ticket ONLY renames a directory. No code modifications.

### Goal
Rename the development folder to sandbox for clarity.

### Commands to Execute
```bash
git mv development/ sandbox/
```

### Files to Modify (references only)
- `CLAUDE.md` - Update references to development/
- `.gitignore` - If development/ is mentioned

### Testing
- [ ] `ls sandbox/` shows previous development/ contents
- [ ] `ls development/` fails (doesn't exist)
- [ ] `poetry run pytest tests/test_structure.py -v`

### Acceptance Criteria
- [ ] `sandbox/` exists with all previous contents
- [ ] `development/` no longer exists
- [ ] Git history preserved
