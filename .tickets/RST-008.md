## RST-008: Flatten remaining projects

### CRITICAL: NO FUNCTIONAL CHANGES
This ticket ONLY moves files and updates imports.

### Goal
Flatten all remaining project folders (simulation, things_behavior, bounds, rsa_comparison, graph_clustering, embedding_generation).

### Projects to Flatten
- projects/simulation/
- projects/things_behavior/
- projects/bounds/
- projects/rsa_comparison/
- projects/graph_clustering/
- projects/embedding_generation/

### Pattern
Same as RST-006 and RST-007:
1. Move lib/*.py to project root
2. Move tasks/*.py to project root
3. Update imports from `..lib.x` to `.x`
4. Delete empty lib/ and tasks/ folders

### Testing
- [ ] Each project importable
- [ ] `poetry run pytest tests/test_structure.py -v`

### Acceptance Criteria
- [ ] No lib/ or tasks/ subfolders in any project
- [ ] All imports resolve
